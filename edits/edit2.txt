//1) почему в Completed пишется 0 items left

2) Сейчас у тебя timer визуальный только. Нужно же чтобы он каждый тик еще обновлял значение таймера для каждой таски в app. Воспроизвести баг сможешь если создашь таску которая закончится через 2 секунды, запустишь таймер, подождешь когда станет 00:00. Переключишься на вкладку completed и обратно. Тогда увидишь что, таймер снова 2 секунды, хотя он уже прошел

//3) в Task сделай условную отрисовку. Если isEdit = true отрисовывается инпут с редоктирование, если false орисовывается другая часть

//4) в NewTaskForm в submitTheForm  в addTodo нужно передавать не e.target, а данные собранные хуком useState. т.е. label минуты и секунды, которые лучше всего преобразовать чисто в секунды

//5) пропсы кстати ты можешь деструктуризовать в самой функции

function Footer( { todos, filter, clearCompleted, onFilterChange }) к примеру

//6) на хуках такого вида запись  

 setTodos((todos) => {

      const newArr = todos.filter((el) => !el.isCompleted);

      return newArr;

    });

выглядит так

const newArr = todos.filter((el) => !el.isCompleted);

setTodos(newArr );